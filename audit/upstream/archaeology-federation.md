# Bounded asset archaeology and native federation evidence

Date: 2026-09-10. Scope: a bounded sub-audit for the first executable Next Internet closure. `sources/` was read only. Three current GitHub defaults were shallow-cloned to `deps/`; supplied archives were inspected and selected code was extracted into `federation/`. No upstream files, pins, branches or Actions were changed.

## Current repository reality

| Asset | Exact observed source | Observed execution | Ruling for this task |
|---|---|---|---|
| Asset Archaeology | `xingxuling/taowind-asset-archaeology`, tarot, `952c4561d04aadc7a7e0eecd797f775d077d2825` | Current CLI init / scan / snapshot / report ran successfully | REUSE for bounded provenance inventory, never Canonical ownership proof |
| DWAC | `xingxuling/DWAC`, main, `4d2990562c4d3a94d0513b99e14ea4ebcf81dea6` | Structural CLI ran; native goal classifier ran | KEEP_SEPARATE for artifact planning/production; classification gap retained |
| USCE | `xingxuling/TaoWind-USCE`, main1, `7b3ab93c2ec6be601456f984963fd872c1885388` | Native query ran; exact DWAC mounting was attempted and rejected | KEEP_SEPARATE for subject federation/reachability; no architectural answer fabricated |
| Reality Network Runtime | Supplied `Reality_Network_Runtime_v0.1.0-alpha.1_完整源码与运行包.zip` | Original 20 Node tests passed locally | KEEP_SEPARATE as RNCS authoritative multiplayer synchronization; ADAPT fault-model and regression ideas |
| Memory World Tree | Supplied `Memory_World_Tree_Five_Level_Formal_Theory_v0.1.0-alpha.1(1).zip` | Original 25 finite-model theorem tests passed locally | KEEP_SEPARATE; ADAPT sovereignty/privacy/evidence distinction regressions |
| BIGS-OS v1.0 | Supplied unified architecture baseline ZIP | Directory/README inspection only | Specification reference; runtime ownership and availability remain UNKNOWN in this sub-audit |
| UPDIA-RNLM lab | Supplied r8c active structural diagnosis ZIP | Inventory/README/packaging inspected; no training run | KEEP_SEPARATE research lab; not a substitute for canonical UPDIA |

The first three clones were clean at the recorded heads. No repository-local AGENTS file was found in those clones. All SHA/command/raw-output evidence is under `raw/`. Latest RNCS and the canonical UPDIA runtime must be resolved by the complementary owner audit: bounded GitHub discovery found `RNCS-Unified-Platform-` (main-95), `zhinao` (main), and `UPDIA-RNLM-Developmental-Lab` (r8c). Exact repository names `RNCS` and `UPDIA` did not resolve; this is not evidence their implementations are absent.

## Asset Archaeology actually executed

The current scanner CLI and source were read before execution. Its archive safeguards are 64 MiB per member, ratio <= 1000, <= 100,000 members and <= 4 GiB accepted uncompressed bytes. This audit stayed far below those ceilings and did not run a whole-drive search.

Explicit inputs:

1. A names/sizes-only manifest of this project's top-level `sources/` files (the manifest itself is one scanned file; the referenced source contents were not all scanned).
2. Downloaded TINP v0.2.0-alpha.1 ZIP, 50 members plus its archive record.
3. The freshly fetched OPP checkout, 113 files; `.git` and generated dependency directories were skipped by the engine.

Result: **165 registrations, 0 scanner errors, 0 skipped entries**; 50 `ARCHIVE_MEMBER`, 1 `BUNDLED_SNAPSHOT`, 114 `LOOSE_DOC`. All 165 have semantic provenance **UNKNOWN**. The legacy report labels them `Historical-evidence eligible`; this means the engine's registry eligibility classification, not verified historical truth, understanding, authority, or RCL promotion.

Raw evidence: `bounded-archaeology.sqlite`, `bounded-snapshot.json`, `bounded-report.md`, and `archaeology-*.command.json` / `*.stdout.txt`. Four separate ZIP directory inventories and selected-source hashes document the smaller Reality/BIGS/World Tree/UPDIA inspections.

## Native organs: actual outcomes, not simulated opinions

**USCE alone:** invoked `usce.cli` with `--query-mode usce`, its checked-in federation manifest, and an architecture/reuse question. It returned `UNRESOLVED`, route `USCE_NATIVE_CAPABILITY_GAP`, kind `GENERAL_TASK_ANSWER_GAP`. `provider_used=false`, `external_provider_used=false`, `world_fact_promoted=false`, `rcl_evidence_commit_performed=false`. Four reachable functions were federation status/closure/projection/composite inspection. The bootstrap parser classified this English request as `CONVERSE_REQUESTER_DELEGATED_OPEN_DOMAIN`; this is an observed parser limitation, not proof that the system can judge the architecture.

**USCE with current DWAC:** mounting the independently current repositories failed closed with `ADAPTER_HANDSHAKE_MISMATCH:dwac:contract_schema,commit,tree,contract,release_receipt,parent_release_receipt,verified_manifest,verified_manifest_file_count,capabilities,declared_capabilities`. No pin was altered and no attestation bypass was applied. This is a concrete integration/version-skew issue for a future bounded task, not a reason to block the internet transport closure.

**DWAC legacy compiler:** the current CLI generated a three-unit structural artifact using `reference.semantic.worker.v0.1`, returning `CANDIDATE` and mechanics acceptance. Source `preartifact.py:from_goal` explicitly creates generic numbered segments. Thus it demonstrates real compiler execution and does **not** supply an independent architecture review.

**DWAC native classifier:** `TheoreticalCoverageCompiler.compile_goal` returned `REPRESENTABLE_PROVIDER_GAP`, generic `custom.future/unknown-artifact`, and the typed need `artifact.intent.semantic-classification`; all nine RCL acceptance gates remained `UNTESTED`. This is the honest native independent classification outcome. Parent-side engineering judgments below are not attributed to DWAC/USCE.

## Reuse and ownership findings from actual code

**Reality Network Runtime** owns a specific authoritative multiplayer synchronization contract: `ServerAuthoritativeWorld` owns one RSR world, accepts bounded player inputs, verifies delegated subject/player/session/action scopes through AAF, produces snapshots/deltas/tick roots, and projects RFE evidence. `LoopbackTransport` and `NetworkConditionSimulator` implement seeded delay/loss/duplication/reordering/bandwidth/disconnect conditions. Recovery is an isolated RBF candidate, not a second authoritative world. Existing tests cover player spoofing, expired/closed authorization, direct client state writes, replay, reconnect, late join, loss recovery and one-world continuity.

This is a valuable **domain organ and stress donor**, not a replacement for TINP's general subject/capability addressing, multi-hop routing or OPP interoperability. `Transport` and `Session` have different scope here: world tick replication/player connection versus general network transactions. Keep these owner-qualified; do not merge because the names match. Simulation truth stays with RNCS/RSR; generic transport does not acquire it.

The **Memory World Tree** source defines `createHumanSubject`, `createWorldTree`, `verifyOntologySeparation`, identity-bound ring-prefix preservation, scoped sharing/revocation and separate contact/belief, hypothesis/reality mechanisms. The 25 tests verify this finite reference model. It is a regression donor for Subject != Node, subject != projection, sovereignty-preserving sharing, and evidence != interpretation; it is not a discovery/routing runtime or proof that a human can be fully modeled.

**BIGS-OS** explicitly says v1.0 is the first frozen unified specification baseline and not a stable production runtime. Its supplied archive provides product/architecture/handoff materials. Do not classify the whole package as an executable kernel from its title.

**UPDIA-RNLM** explicitly says it is an isolated CPU research lab, not a production UPDIA replacement. Source inventory includes experiments, model configurations, lineage/evidence and intervention studies. Its README preserves negative developmental recovery results and the authority boundary: predictions/proposals do not own world truth, identity roots, durable memory, mutation acceptance, merge or external action. Reuse this boundary/regression discipline; importing a trained lab is unnecessary for the first deterministic network closure.

## Runtime evidence and its limits

| Check | Current result | Exact scope |
|---|---|---|
| Reality network upstream tests | 20 passed, 0 failed | In-process deterministic loopback; local RNCS/AAF/RSR/RFE/Gateway modules |
| World Tree upstream tests | 25 passed, 0 failed | Executable finite model; no user study or production world federation |
| USCE native question | Executed / UNRESOLVED | Provider-free subject query |
| USCE-DWAC current mount | Executed / rejected | Exact handshake mismatch; no fallback |
| DWAC structured compiler | Executed / CANDIDATE | Three reference semantic units, mechanics only |
| DWAC unknown-goal classifier | Executed / PROVIDER_GAP | No domain architecture classification |

Local workspace dependency links were created only under the extracted Reality audit directory. No registry installation or package lifecycle scripts ran. The original selected source/tests were not edited. One Windows console encoding exception happened **after** the World Tree subprocess completed; its already-captured exit code/output were preserved and no failed test was relabeled. The runner was corrected to print UTF-8 and resumed only the unrecorded network test.

No result here establishes public Internet reachability, NAT traversal, multi-host execution, production authentication, military-grade reliability, runtime cost guarantees, formal K400 PASS or canonical promotion.

## License and integration court

The archaeology repository contains an Apache-2.0 license declaration and license file. Reality Network Runtime and World Tree package metadata declare Apache-2.0. DWAC structural generation and USCE have no root LICENSE found and no license field in the inspected pyproject files; their separate license-audit evidence is not treated as a blanket redistribution grant. This audit ran the user's sources locally and did not copy their implementation into the new product. Any later source/package redistribution should preserve actual upstream notices and resolve unspecified licensing explicitly.

Integration decision: use these assets as **separate sources of capability, test cases and evidence**. Do not import the three organ implementations into the product to make the architecture look integrated. The earliest useful closure remains owner-preserving deterministic capability execution over TINP/OPP with explicit authority checks and receipts. USCE/DWAC architecture gaps and exact-mount mismatch remain recorded side issues; no generic model substitutes for them.

RCL pressure evidence: cross-language owner-qualified session/subject/authority/evidence references, rejection of current-version federation drift, and fault injection without truth/authority promotion are concrete regression candidates. This sub-audit does not establish a missing RCL Primitive/IR/Runtime/Profile and therefore creates no fabricated formal RCL_GAP. Numeric K400 coordinates are delegated to the audited RCL matrix; relevant axes are network/distributed execution, authorization/evidence and degraded transport, with all nine promotion gates unadjudicated here.

## Memory provenance used only as search guidance

`C:/Users/User/.codex/memories/MEMORY.md:602-614` informed candidate-only and no-provider-substitution checks. Current outcomes above were independently refreshed. Associated rollout: `01a04f90-e871-79f1-899c-07ec598aed8e`. No historical test count or Git head was used as current proof.
