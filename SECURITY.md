# 安全说明

TINP 当前状态是 **VERIFIED_LOCAL_CANDIDATE / NOT_DEPLOYED**。它已经有本机受控环境下的安全与恢复验证，但不应被描述成已经完成生产安全认证的网络。

## 报告安全问题

如果你发现可能影响以下边界的问题，请不要在公开 Issue 中直接发布可利用细节：

- 身份或签名验证绕过；
- 权限租约绕过；
- Operator Approval 绕过；
- Authority Registry 回退或分叉检查绕过；
- Pending 请求被静默重发；
- 回执 / Evidence Ledger 完整性绕过；
- Recovery Anchor 回退检测绕过；
- TLS / transport policy 绕过；
- DPAPI 持久状态泄漏；
- 任何导致权限在恢复后扩大的问题。

如果仓库启用了 GitHub Private Vulnerability Reporting，请优先使用该入口。若没有启用，可以先开一个不包含利用细节的 Issue，请求建立私下沟通渠道。

## 在线准入和离线校验不是一回事

在线节点在 `src/node-process.mjs` 的 `execute()` 中检查请求、租约、会话、签名、权限、撤销状态、契约及路由约束；RCL 检查未通过时直接拒绝，业务计算位于这些检查之后。调用端的预检不能代替执行端检查。

执行后，`src/suite.mjs` 的 `verifyReceipt()` 核对回执签名及请求、会话、租约、输入和结果的绑定。核对失败意味着结果不能被接受，不意味着已经发生的动作被自动撤销。当前纯计算示例的去重机制也不能外推为付款、删文件等外部动作的“只执行一次”保证。

`src/opp-native-interop.mjs` 等离线校验入口只处理已有证据，不是执行前拦截器。已有回执通过校验，不会补发授权，也不会证明原始执行全过程都经过了 TINP。

## 防绕过边界

当前前置检查覆盖的是 TINP 管理的执行入口，不是整个操作系统。调用者若另有数据库凭据、设备控制接口或直接调用底层服务的权限，不能靠外层校验函数自动封住这些路径。

生产接入需要把真实资源权限绑定到受控执行入口，并审查旁路、密钥和策略修改权限。长期任务、外部事务以及检查后状态发生变化的情况，还需要按业务设计再次检查、事务或隔离机制。HSM（硬件安全模块）本身不等于完成这些工作。

本文所说的“允许”是符合已配置的权限和策略，不是自动判断现实法律合规。当前也不把整个 TINP 工具包称为已经实现了不可绕过的操作系统安全内核。

实现位置、现有测试和参考监控器设计要求见 [执行前约束与执行后核对](docs/ENFORCEMENT_AND_EVIDENCE.md)。

## 当前安全边界

当前不声称：

- 生产级 HSM / 硬件密钥托管；
- 可信外部时间；
- 公网规模身份与撤销服务；
- 互联网规模共识或状态收敛；
- 第三方安全认证；
- 军用安全认证。

本机 loopback、临时证书、测试签署方和 Windows 当前用户 DPAPI 都只是当前验证环境的一部分，不代表生产部署方案。

详细边界见 [`docs/EXTERNAL_ACCEPTANCE_GATES.md`](docs/EXTERNAL_ACCEPTANCE_GATES.md)、[`docs/LICENSE_AUDIT.md`](docs/LICENSE_AUDIT.md) 和各版本 `evidence/`。
