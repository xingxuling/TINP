# TINP Roadmap

这不是发布日期承诺，而是根据当前 `docs/NEXT_GAP.md`、`README.md` 和 alpha.29 证据整理出的**下一阶段真实缺口**。

## 从试点到生产的最短路径

```text
当前：VERIFIED_LOCAL_CANDIDATE
        ↓
稳定第三方集成接口
        ↓
真实两台设备闭环
        ↓
生产 Authority + 密钥生命周期
        ↓
可信时间 + 独立第三方接入
        ↓
独立安全评估
        ↓
才讨论生产部署
```

每一层都应该有对应的可复核 evidence；不因为上一层 PASS 就自动跳过下一层。

## P0 — 稳定外部集成接口

alpha.29 已经有 `InternetSuite`、OPP acceptance、HTTP read-only adapter 和仓库内部 Provider 路径，但还没有稳定的第三方 Provider SDK。

进入外部 PoC 前，需要先收敛：

- Caller public surface；
- Provider manifest 最小字段；
- capability / OPP 映射；
- handler 输入输出约束；
- receipt 稳定字段；
- 错误码与恢复语义；
- 版本兼容策略；
- 一套最小第三方 Provider 示例。

这一步的目标不是“冻结所有内部实现”，而是让外部团队不必修改 `src/node-process.mjs` 才能接入。

当前集成边界见 [`docs/INTEGRATION.md`](docs/INTEGRATION.md)。

## P0 — 真实跨设备验证

当前多数传输、恢复和收敛证据来自本机多进程与 loopback。

下一阶段最有价值的验证不是继续增加本机夹具，而是：

- 两台真实设备；
- 独立密钥；
- 加密连接；
- 真实中断；
- 设备重启；
- Provider 切换；
- 恢复后权限不扩大；
- 全程保留可复核回执。

只有完成这一步，才能把“跨进程可运行”推进到“跨设备可运行”。

## P0 — 生产 Authority Owner

当前 Authority Registry、撤销、见证和时间等关键输入仍主要由调用方提供。

需要明确一个真实的生产 Owner，至少解决：

- 身份如何注册；
- 谁能签发权限；
- 权限怎么撤销；
- 密钥丢失怎么恢复；
- 成员轮换怎么处理；
- 冲突和分叉由谁裁决。

在 Owner 没有确定之前，不应把本机 registry 叫作生产权限服务。

## P0 — 密钥生命周期

当前测试签署方、临时 TLS 证书和 Windows DPAPI 都只是验证环境的一部分。

生产化需要单独解决：

- 密钥生成；
- 安全保存；
- 轮换；
- 撤销；
- 设备迁移；
- 丢失恢复；
- 操作员密钥与运行节点隔离。

HSM 或其他硬件托管是否需要，应由目标部署环境决定，而不是默认宣称。

## P1 — 可信时间与透明历史

当前还没有可信外部时间，也没有生产级透明日志。

下一阶段需要验证：

- 旧状态是否能被可靠识别；
- 撤销顺序是否能被独立复核；
- 不同主机看到的 Authority 历史是否一致；
- 见证文件和本地 store 同时被替换时怎么检测。

## P1 — 独立第三方接入

alpha.29 已经能绑定 OPP native interop 的本机 acceptance，但这仍不是独立第三方 consumer。

目标是让一个不属于当前仓库的项目：

- 读取 OPP 能力；
- 通过 TINP 发起受控调用；
- 接受或拒绝结果；
- 独立复核 receipt；
- 在真实故障后完成恢复。

## P1 — 更真实的网络条件

在真实跨设备闭环之后，再逐步测试：

- 延迟；
- 丢包；
- 临时断网；
- 网络切换；
- 节点长时间离线；
- 旧配置恢复；
- 多节点同时变化。

重点不是追求“永远自动恢复”，而是确保失败状态明确、权限不漂移、证据可复核。

## P2 — 独立安全评估

在真实设备、生产 Owner 和密钥生命周期稳定之后，再进入：

- 威胁建模；
- 独立代码审查；
- 渗透测试；
- 部署基线；
- 运维与事件响应流程。

在完成相应评估前，不宣称生产安全认证、互联网规模能力或军用安全认证。

## 当前不作为短期目标

- 用“新互联网”口号代替跨设备证据；
- 为了自动化而静默重试高影响动作；
- 在没有生产 Owner 时自动指定信任根；
- 把本机 loopback 结果当成公网结果；
- 在没有真实试点数据时写无证据的性能或 ROI 数字。

当前最短真实缺口仍以 [`docs/NEXT_GAP.md`](docs/NEXT_GAP.md) 为准。
